# GitHub OAuth App: https://github.com/settings/developers
GITHUB_CLIENT_ID=Ov23li6GZwIMImb41Q4o
GITHUB_CLIENT_SECRET=083832617185d12365d08e59ce33b4cc5f4d5c3b
# Must match the Authorization callback URL in the GitHub OAuth app (e.g. http://127.0.0.1:8000/auth/callback)
GITHUB_REDIRECT_URI=https://aventisia.onrender.com/auth/callback
# Random secret for session signing (e.g. openssl rand -hex 32)
# SESSION_SECRET=
# # On Render (HTTPS), set so the session cookie is Secure
# HTTPS_ONLY=true
# # Optional: public URL for OpenAPI "Servers" in /docs (defaults to current origin)
# # PUBLIC_BASE_URL=https://your-service.onrender.com
# # Optional: comma-separated browser origins (CORS + credentials for session cookie)
## CORS_ORIGINS=https://your-frontend.example.com,http://localhost:3000
